fun check(number) {
    if number == 1 {
        println(1)
        return "Okay"
    }
    elif number == 2 {
        println(2)
        return "Okay"
    }
    else {
        println("Argument is not 1 nor 2")
        return "Error"
    }
}

; check(1)
; check(2)
; check(3)

fun getReturnValue {
    return 1 == 1
}

println(getReturnValue())
