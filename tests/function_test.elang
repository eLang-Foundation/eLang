; testing function without arguments
; fun without_arguments {
	; println("hello, world")
; }

; without_arguments()

; testing function with one argument
; fun one_argument(name) {
	; println("hello, ", name)
; }

; one_argument("Bekhruz")

; testing function with multiple arguments
; fun multiple_arguments(first_name, last_name) {
	; println("Hello, ", first_name, " ", last_name, "!")
; }

; multiple_arguments("Bekhruz", "Niyazov")

fun test(i) {
	if i == 1 {
		println("Hello, world!")
	}
}
test(0)

; testing return statements
fun return(i) {
	if i == 1 {
		return "hell, world"
	}
	else {
		return "goodbye world"
	}
}

println(return(1))
println(return(2))
